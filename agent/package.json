{
  "name": "hive-digest-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Generate and email Hive Digest (Synbrains) newsletters via SMTP",
  "scripts": {
    "start": "node src/run.mjs",
    "generate": "node src/run.mjs --dry-run",
    "test": "node src/research.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "nodemailer": "^9.0.3"
  }
}
