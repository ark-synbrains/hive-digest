{
  "name": "dev-digest-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Generate and email /dev/digest technical newsletters via SMTP",
  "scripts": {
    "start": "node src/run.mjs",
    "generate": "node src/run.mjs --dry-run"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "nodemailer": "^9.0.3"
  }
}
