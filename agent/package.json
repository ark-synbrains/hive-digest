{
  "name": "hive-digest-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hive Digest Node sender — research, validate/rank, and email issues via SMTP (hive.synbrains.ai)",
  "scripts": {
    "start": "node src/run.mjs",
    "generate": "node src/run.mjs --dry-run",
    "test": "node src/research.test.mjs && node src/digest.test.mjs && node src/graphrag.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "nodemailer": "^9.0.3"
  }
}
