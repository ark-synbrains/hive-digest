# Never index Cursor/agent rules or Graphify skill tooling into the project graph.
# "Rules" must not become graph nodes — keep these patterns broad on purpose.
.cursor/rules/
.cursor/**/*.mdc
.cursorrules
**/.cursorrules
**/.cursor/rules/
.agents/
**/.agents/
**/AGENTS.md
**/CLAUDE.md
**/RULES.md
**/rules.md

# Local / ephemeral run outputs and unused alternate GraphRAG paths
agent/out/
agent/digest-corpus/
agent/digest-graph/

# Issued newsletter archives are git-tracked but excluded from the *codebase* graph
digests/

# Package manifests (noisy leaf nodes; not useful for architecture queries)
package.json
**/package.json
package-lock.json
**/package-lock.json
